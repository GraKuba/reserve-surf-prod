"""
Users app URLs.
"""

from django.urls import path
from . import views

app_name = "users"

urlpatterns = [
    # Add user-related endpoints here
    # Example: path("profile/", views.UserProfileView.as_view(), name="profile"),
]